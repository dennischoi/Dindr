<h1>Top Restaurants</h1>

<p>look at my list!!</p>

<div class="yelp-results">
  <% @restaurant_list.each do |name, url| %>
    <ul>

      <li>
        <%= link_to "#{name}", url %>
      </li>
   </ul>
  <% end %>
</div>
